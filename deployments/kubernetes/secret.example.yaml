# Example only. Not included in kustomization.yaml on purpose: create the
# real secret out of band (kubectl create secret, Sealed Secrets, External
# Secrets, ...). Never commit real values.
apiVersion: v1
kind: Secret
metadata:
  name: incident-investigator
  namespace: incident-investigator
type: Opaque
stringData:
  DATABASE_URL: postgres://USER:PASSWORD@HOST:5432/investigator?sslmode=require
  OPENAI_API_KEY: ""
