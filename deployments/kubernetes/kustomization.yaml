apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: incident-investigator
resources:
  - namespace.yaml
  - configmap.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
# Pin the image per environment, e.g.:
# images:
#   - name: ghcr.io/kmpoltorak/ai-network-incident-investigator
#     newTag: v0.1.0
