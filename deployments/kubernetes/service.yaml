apiVersion: v1
kind: Service
metadata:
  name: incident-investigator
  labels:
    app.kubernetes.io/name: incident-investigator
spec:
  selector:
    app.kubernetes.io/name: incident-investigator
  ports:
    - name: http
      port: 80
      targetPort: http
